# Obelisk Doc Index
Generated: 2026-09-01

Auto-generated by AI. Used as navigation index for AI context selection. Used to select relevant docs only instead of reading all design docs — to reduce cost and increase accuracy. Snapshot of docs at generation time — regenerate by requesting "regenerate doc index" in any task when docs change significantly.

## Doc Paths
Folders to search recursively for design docs:
- design-docs/

---

### architecture_overview.md
About: Platform, packages, layer rules, feature boundaries, and Riverpod conventions. Read before any other doc.
Covers: Flutter Windows desktop, Riverpod 3.0 (AsyncNotifier/Notifier), go_router, sqflite_common_ffi, layer architecture (Presentation/Application/Domain/Data), key rules on data flow and report storage.
Relevant for: Every task — foundational rules that govern all implementation.
Depends on: none

### main_workflow.md
About: End-to-end description of the 9-stage report generation pipeline (schedule detection now absorbs shift period construction, so there is no separate extraction stage).
Covers: Stage 1–9 flow (file upload → generate trigger with tolerance re-check → dictionary build → schedule detection incl. shift period construction → off-day detection → daily period extraction with month trim → shift overtime calc → daily overtime calc → store & navigate), error handling, key design principles, background isolate CPU offload for the CPU-bound stages.
Relevant for: Any task touching report generation, pipeline stages, or generation service.
Depends on: architecture_overview.md

### config.md
About: All constants and configurable defaults — the single source of truth for setting keys and values.
Covers: Daily settings, shift settings (shift_edge_tolerance/shift_inner_tolerance/shift_duration_tolerance split, baseline/ceiling hours), display settings (rounding mode, max date range), tolerance ≤ zone_interval/2 constraint (enforced at config time and generation time), hardcoded constants (off_day_threshold, detection_edge_tolerance, morning_arrival_lead, max_look_ahead_days, min_morning_days), default column headers.
Relevant for: Settings screen, configuration repository, any stage that reads settings.
Depends on: none

### data_shared_models.md
About: Domain models used across the pipeline — plain data containers only.
Covers: ShiftEmployeeResult, DailyEmployeeResult, UndetectedEmployeeResult, ShiftPeriod, DailyPeriod — fields, who sets them, isValid vs isIncluded distinction, notes as a Set of Arabic reasons (not a single string), zoneResults carrying stored windowStart/windowEnd.
Relevant for: Any task creating or consuming pipeline data models, repositories, or display logic.
Depends on: architecture_overview.md

### database_schema.md
About: All SQLite tables, columns, relationships, and versioning strategy.
Covers: reports, shift_employee_results, shift_period_details, daily_employee_results, daily_period_details, undetected_employee_results, column_headers, app_settings — schema, cascade rules, seeding, settings reconciliation on upgrade (schema v6, drop-unknown/seed-missing plus onOpen reseed), notes stored as a JSON array of reasons, zone_data carrying windowStart/windowEnd, why report data is dropped rather than migrated, data loading rules at display time. Does not document the FK-column indexes added in the 20260901-1400 task — that was an undocumented oversight fix, not a design change.
Relevant for: Database initialization, repositories, any task reading/writing reports or settings.
Depends on: data_shared_models.md, config.md

### dictionary_build.md
About: Stage 3 — builds the working dictionary from attendance files and the selected date range.
Covers: Single-pass record collection, timestamp normalization, sort step, output format.
Relevant for: Dictionary build function implementation, generation service orchestration.
Depends on: main_workflow.md, file_processing.md

### file_processing.md
About: Stage 1 — attendance Excel file validation on upload.
Covers: Supported formats (.xlsx/.xls), multi-sheet, multi-file behavior, column header validation, valid row definition, validation error messages.
Relevant for: File picker, file validation service, report generation screen.
Depends on: config.md, screen_report_generate.md

### schedule_detection.md
About: Stage 4 — the unified V2 algorithm: detects each employee's employment type (shift/daily/undetected), shift start time, and builds shift periods in the same pass.
Covers: Zone layout (centers → midpoint buckets → separate classification/overtime validity windows), detection_edge_tolerance vs shift_edge_tolerance split, per-day zone check plus anchor pair fallback contributing to the same period bucket, winner-vs-runner-up start-time confidence margin, daily validation gate (one-sided entry window, open-working-days threshold with floor of 5), failure reasons, output three buckets. Supersedes period_extractor_shift.md (retired).
Relevant for: Schedule detection function implementation, generation service.
Depends on: dictionary_build.md, config.md

### off_day_detection.md
About: Stage 5 — detects off-days from daily employee attendance density.
Covers: Input (daily hash table), output (off-day date hash set), attendance rate formula, threshold behavior (strict less-than), minimum employee guard.
Relevant for: Off-day detection function, generation service.
Depends on: schedule_detection.md, config.md

### period_extractor_shift.md
About: RETIRED — merged into schedule_detection.md (Detection V2). Kept as a record of the original design; do not implement from it.
Covers: Zone layout formula, period window definition, period separation, zone bucketing, isSatisfied flag, shared timestamp rule, discard weak periods (< 2 zones satisfied) — this threshold is stale, see schedule_detection.md for the live min_zones_satisfied rule.
Relevant for: Nothing current — read schedule_detection.md instead.
Depends on: schedule_detection.md, data_shared_models.md, config.md

### period_extractor_daily.md
About: Stage 6 — extracts DailyPeriod objects from the daily hash table.
Covers: Calendar-day grouping, off-days hash set consumption, day type classification, DailyPeriod base field construction, report-month trim (drops days spilling into the next month, bounded by max_look_ahead_days, so shift look-ahead days aren't double-paid to daily employees).
Relevant for: Daily period extractor function, generation service.
Depends on: off_day_detection.md, data_shared_models.md

### overtime_calculation_shift.md
About: Stage 7 — validates ShiftPeriod objects and calculates per-employee shift overtime.
Covers: Zone center definitions, validity rule (every applicable check runs unconditionally, failures collect into a notes set, valid only when the set is empty), hoursCounted (24/0 binary), per-employee overtime formula (cap at ceiling, subtract baseline), output enriched hash table. Note: doc content predates the reason-set change described in history — verify against schedule_detection.md's shared zone layout before implementing.
Relevant for: Shift calculator function, generation service.
Depends on: period_extractor_shift.md, config.md

### overtime_calculation_daily.md
About: Stage 8 — validates DailyPeriod objects and calculates per-day and per-employee daily overtime.
Covers: Regular day rules (≥2 timestamps, delay allowance), off day rules, overtime formula (lastTimestamp − endTime for regular, lastTimestamp − firstTimestamp for off), daily max cap, invalid reasons collected into a notes set (not a single string — a stale sentence about computing overtime live at display time is superseded by data_shared_models.md/report_calculation.md, which are authoritative: total is computed at generation time and stored).
Relevant for: Daily calculator function, generation service.
Depends on: period_extractor_daily.md, config.md

### report_calculation.md
About: How report data is loaded from the database and how summary values are assembled for display — display is purely passive, no business logic runs at load time.
Covers: Report screen load (three parallel queries by reportId, periods loaded lazily), detail screen load, shift/daily per-employee and summary-card values (all read from stored fields, simple addition only), rounding modes applied at display/export time only, is_included toggle behavior (in-memory running-total adjustment, no DB re-fetch), undetected employees (never contribute to summaries/export), export behavior (always the full included set regardless of active search filter).
Relevant for: Report screen, detail screen, export logic, any task touching how stored results are read and summarized for display.
Depends on: data_shared_models.md, database_schema.md, architecture_overview.md

### router.md
About: Navigation structure — routes, parameters, and navigation rules.
Covers: Route table, navigation flow, route parameter definitions. Note: describes the original two-tab shell — history shows this was replaced by a flat router (generation screen as root, history/settings reached via drawer with push navigation) and an undetected route was added nested under report; verify current routes in lib/core/router/router.dart before relying on this doc for the shell shape.
Relevant for: App shell setup, go_router configuration, any screen navigation.
Depends on: architecture_overview.md

### screen_report_generate.md
About: Report Generation screen — file upload and date range selection UI.
Covers: File list card (empty/with-files states, file rows, max 10 files, info hint), date range pickers with validation, generate button enabled condition, loading/success/failure states, screen state management. Note: history shows the modal barrier during generation was replaced by an animated GenerationOverlay widget — check lib/features/reports/screens/ for the current loading UI.
Relevant for: Report generation screen widget and provider.
Depends on: file_processing.md, router.md

### screen_history.md
About: Reports List screen — lists all generated reports.
Covers: Reports table (generation datetime, period dates, delete action), row tap navigation, delete confirmation, data source (loaded on tab open/return). Note: history shows the FAB-based entry point was removed when the generation screen became the app root; the list is now reached via the drawer.
Relevant for: Reports list screen widget and provider.
Depends on: router.md, database_schema.md

### screen_report.md
About: Report screen — displays results of one generated report.
Covers: Data loading (three parallel queries by reportId, periods loaded lazily), summary cards, filter bar, employee tables, inclusion toggle, row tap to detail, sorting, empty states, export format. Note: doc describes an earlier three-tab-with-radio-filters design; history shows this evolved substantially — segmented shift/daily toggle instead of tabs, undetected moved to its own route, filters moved inline under column headers as dropdowns/checkboxes (not radio buttons), 4 summary cards including gross vs deserved overtime, daily overtime split into regular/off/total columns. Read screen_report.md for data-loading and export-behavior fundamentals (still accurate per report_calculation.md); verify current filter/layout shape in lib/features/reports/screens/report_screen.dart and recent history entries before implementing UI changes.
Relevant for: Report screen widget, report provider, export logic — especially load/filter/refresh behavior.
Depends on: data_shared_models.md, database_schema.md, router.md, config.md, report_calculation.md

### screen_detail.md
About: Detail screen — period-by-period breakdown for one employee.
Covers: Timestamp display rule (time only, Arabic locale), employee header (shift vs daily), daily period table columns, shift period table columns with zone display, row color coding, invalid reasons. Note: history shows several zone-display refinements (window+timestamp inline display, window/outside-window column split) and an added export button — verify current shape in lib/features/reports/screens/detail_screen.dart.
Relevant for: Detail screen widget and provider.
Depends on: data_shared_models.md, database_schema.md, router.md

### screen_configuration.md
About: Settings screen — all user-configurable settings inline.
Covers: Daily settings section, shift settings section (including multi-value shift_start_times and the tolerance ≤ interval/2 config-time guard), display rounding mode (radio group), column headers management (default locked, user-added editable/deleteable).
Relevant for: Settings screen widget and settings provider.
Depends on: config.md, database_schema.md

### Presentation Notes.md
About: Informal pitch/demo talking points for presenting the app — not a technical design doc.
Covers: Problem statement (manual overtime calc pain points), demo script beats (generate → animation → report walkthrough → export → undetected → settings → history), a duplicate-file robustness talking point, a digital-transformation pitch (speed/accuracy gains).
Relevant for: Nothing implementation-related — skip for engineering tasks; only relevant if asked to help with a demo/pitch.
Depends on: none

---

## Regeneration Notes
- Two files present in design-docs/ were missing from the previous index snapshot (an oversight, not a recent addition): report_calculation.md and Presentation Notes.md. Both are now included above.
- Several docs (config.md, main_workflow.md, data_shared_models.md, database_schema.md, dictionary_build.md, off_day_detection.md, schedule_detection.md, period_extractor_daily.md) were edited after the previous index was generated (2026-08-31, tolerance-split task) and have been re-summarized against their current content.
- screen_report.md, screen_detail.md, screen_history.md, screen_report_generate.md, router.md are known to have drifted from the live UI per obelisk/history.md (filter mechanics, navigation shell, tab structure) — their entries now flag this explicitly. Doc content always takes precedence, but for these five, recent history.md entries or the current source are the more reliable source for exact current UI shape.
